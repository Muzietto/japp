(function() {
  'use strict';
  
  define([], function() {
    
    // promise()
    // fulfill(promise, value)
    // depend(promise, expression)
    
    
    return {
      pippo: 'stringa pippo'
    };
  });
  
})();